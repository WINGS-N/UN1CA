on early-init
    exec u:r:init:s0 root root -- /vendor/bin/tee.sh

on fs && property:dev.teegris.model=old
    mount none /vendor/etc/tee_old /vendor/tee bind

on fs && property:dev.teegris.model=eyb1
    mount none /vendor/etc/tee_eyb1 /vendor/tee bind
